#!/bin/sh

case "$DEBUG" in
  "denvig:*"|"denvig:cpu") CPU_PROF="--cpu-prof" ;;
  *) CPU_PROF="" ;;
esac

# shellcheck disable=SC2068,SC2086
node $CPU_PROF --experimental-strip-types packages/cli/src/cli.ts $@
