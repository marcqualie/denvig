{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://denvig.com/schemas/config.global.json",
  "title": "Denvig Global Configuration",
  "description": "Configuration schema for global denvig config files",
  "type": "object",
  "properties": {
    "codeRootDir": {
      "type": "string",
      "description": "The root directory where all code is stored"
    },
    "quickActions": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Actions that are available on the CLI root for quick access"
    }
  },
  "additionalProperties": false,
  "required": [
    "codeRootDir"
  ]
}
